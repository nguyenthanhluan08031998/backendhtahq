import * as dataService from "@/app/utils/dataService"

const controller = {
    signUp: 'hash'
}

export const signUp = (user) => {
    return dataService.postFunction(controller.signUp, `addOrUpdate`, user)
}



